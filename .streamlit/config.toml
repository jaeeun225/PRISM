[server]
# Compress the Streamlit WebSocket payload, including Plotly figure JSON.
enableWebsocketCompression = true

[theme]
# Keep Streamlit controls and loading indicators on the PRISM light-blue palette.
base = "light"
primaryColor = "#4FC3F7"
