# Copy to .streamlit/secrets.toml locally. Never commit real credentials.
SUPABASE_URL = ""
SUPABASE_SERVICE_KEY = ""
SUPABASE_BUCKET = "prism-data"
SUPABASE_OBJECT_PATH = "fragella_processed.csv.gz"

# Bump after replacing the CSV, or clear/reboot the app cache.
SCENT_DATA_VERSION = "1"
